# Episode: Costes 3B

## Fecha

2026-06-05


## Tarea

Implementar COSTES-3B: revisión operativa del módulo de costes antes de vincularlo con patologías.

## Plan asociado

costes-3b.md


## Task Pack usado

`docs/harness/TASK_PACKS/db_change.md`

## Objetivo

Pulir uso real de costes: parser OCR para captura IVE `ERPG.4aba`, trazabilidad de fuentes en detalle y checks de listado/validación, manteniendo el módulo aislado.

## Archivos modificados

- `app/services/costes_parser.py`
- `app/routers/costes.py`
- `templates/costes/detalle.html`
- `tests/smoke/test_costes_capturas.py`
- `tests/smoke/test_costes_workbench.py`
- `docs/harness/PLANS/active/costes-3b.md`
- `docs/harness/EPISODES/2026-06-05-costes-3b.md`

## Validaciones ejecutadas

- `python3 scripts/audit_docs.py`
- `python3 -m compileall app`
- `.venv/bin/python -m pytest tests/smoke/test_costes_capturas.py tests/smoke/test_costes_workbench.py -q`
- `.venv/bin/python -m pytest tests/smoke/test_costes_db.py tests/smoke/test_costes_workbench.py tests/smoke/test_costes_capturas.py tests/smoke/test_costes_bc3.py -q`
- `git diff --check`
- `bash scripts/finish_harness_task.sh`
- `bash scripts/finish_harness_task.sh` ejecutó además compile de tests, `node --check` de shell/PWA/service worker y smoke completo: 108 passed.

## Resultado

Parser OCR ajustado para:
- importes con decimal de punto (`16.77`) sin romper miles españoles (`1.234,56`);
- líneas de descomposición con unidad tras código (`MOOA.8a h ...`);
- descripción técnica más limpia, evitando cabeceras de tabla.

Detalle de partida enriquecido:
- origen de base visible;
- bloque de fuente/trazabilidad con captura o BC3 cuando exista;
- suma, precio y diferencia siguen visibles;
- botón claro de validar partida.

## Warnings

La extracción OCR sigue siendo asistida: prellena, advierte y exige revisión manual. No valida ni conecta con patologías.

## Rollback

Revertir los archivos listados. No hay cambios de esquema.

## Memoria actualizada

No se actualiza memoria global. Se documenta que costes sigue aislado de patologías, informes, facturación y expedientes.

## Decisiones humanas

No requeridas.

## Proximos pasos

COSTES-4: vinculación controlada con patologías.
